# Instruction coverage

> Status: **reconciled against the complete §12.3 summary and individual
> status tables through Task 0135**.
> Implemented rows are backed by named self-checking tests unless explicitly
> marked otherwise. Known gaps and noncompliant behavior are recorded rather
> than counted as complete.

## How this table is maintained

When an instruction is added (decode + execute), one row is added here.
**Never silently stub.** If decode recognizes the opcode but execute is
unimplemented, the instruction is added with `Status = decoded, traps as illegal`.

Required columns:

- **Mnemonic** — assembler form.
- **Encoding** — opcode bit pattern, with operand-bit placeholders.
- **Source** — page/section in the 1988 User's Guide (or other doc in
  `third_party/TMS34010_Info`).
- **Status** — `not started` | `decoded` | `implemented` | `verified`.
- **Test** — name of the test (or `none` if not yet covered).
- **Flags** — flags written (N/V/C/Z, plus any '34010-specific bits).
- **Memory** — `none`, `read`, `write`, `rw`, `pixblt`, `linedraw`, etc.
- **Cycles** — cycle count if known from the spec; `unknown` otherwise.
- **Notes** — known limitations, deferred operand modes, etc.

## Table

| Mnemonic | Encoding | Source | Status      | Test | Flags | Memory | Cycles  | Notes |
|----------|----------|--------|-------------|------|-------|--------|---------|-------|
| MOVI IW  | `0x09C0 \| (R<<4) \| N`, +16-bit imm | 1988 User's Guide page 12-159 (A0011/A0012) | verified | tb_movi, tb_movi_flags | **N, Z, V=0; C Unaffected** | none | TBD | Sign-extend the 16-bit immediate to 32 bits → Rd, then update N/Z, preserve C, and clear V. |
| MOVI IL  | `0x09E0 \| (R<<4) \| N`, +32-bit imm (LO,HI) | 1988 User's Guide page 12-160 (A0011/A0012) | verified | tb_movi_il, tb_movi_flags | **N, Z, V=0; C Unaffected** | none | TBD | Low-word-first 32-bit immediate → Rd, then update N/Z, preserve C, and clear V. |
| MOVK     | `0x1800 \| (K<<5) \| (R<<4) \| N`, single word | 1988 User's Guide page 12-161 (A0013) | verified | tb_movk | **none** (all Unaffected) | none | TBD | Unsigned constant 1..32 → Rd; opcode K field zero represents 32, so literal zero is not encodable. Exact zero-field result is tested. |
| ADD Rs,Rd | `0100 000S SSSR DDDD` (= `0x4000 \| (S<<5) \| (R<<4) \| D`) | SPVU001A A-14 (A0014, A0015) | implemented | tb_add_rr | N, C, Z, V | none | TBD | Rs + Rd → Rd. First reg-reg arithmetic. Rs and Rd share file (single R bit). |
| ADDC Rs,Rd | `0100 001S SSSR DDDD` (= `0x4200 \| (S<<5) \| (R<<4) \| D`) | SPVU001A page 12-37 + summary table | implemented | tb_addc_subb | N, C, Z, V | none | TBD | Rs + Rd + C → Rd (carry-in from ST.C). Used for extended-precision chains with ADD/ADDI/ADDK. Default operand routing (alu_a=Rs, alu_b=Rd); commutative so no swap. |
| SUB Rs,Rd | `0100 010S SSSR DDDD` (= `0x4400 \| (S<<5) \| (R<<4) \| D`) | SPVU001A A-14 | implemented | tb_sub_rr | N, C, Z, V | none | TBD | Rd - Rs → Rd. ALU operand swap (alu_a=Rd, alu_b=Rs) handled in the core. C is borrow output. |
| SUBB Rs,Rd | `0100 011S SSSR DDDD` (= `0x4600 \| (S<<5) \| (R<<4) \| D`) | SPVU001A page 12-248 + summary table | implemented | tb_addc_subb | N, C, Z, V | none | TBD | Rd - Rs - C → Rd (borrow-in from ST.C). Same operand-swap as SUB. Spec page 12-248 supplies 14 authoritative test vectors; tb_addc_subb uses row 7 (`0x7FFFFFFE - 0xFFFFFFFE` with C=0 → `0x80000000`, NCZV=1101) as a signed-overflow corner-case check. |
| AND Rs,Rd  | `0101 000S SSSR DDDD` | 1988 User's Guide page 12-42 | verified | tb_logical_rr, tb_logical_flags | **Z only; N/C/V Unaffected** | none | TBD | Rd & Rs → Rd. Commutative; default operand routing. |
| ANDN Rs,Rd | `0101 001S SSSR DDDD` | 1988 User's Guide page 12-44 | verified | tb_logical_rr, tb_logical_flags | **Z only; N/C/V Unaffected** | none | TBD | Rd & ~Rs → Rd. Non-commutative — uses the same alu_a/b swap as SUB. |
| OR Rs,Rd   | `0101 010S SSSR DDDD` | 1988 User's Guide page 12-172 | verified | tb_logical_rr, tb_logical_flags | **Z only; N/C/V Unaffected** | none | TBD | Rd \| Rs → Rd. Commutative. |
| XOR Rs,Rd  | `0101 011S SSSR DDDD` | 1988 User's Guide page 12-255 | verified | tb_logical_rr, tb_logical_flags | **Z only; N/C/V Unaffected** | none | TBD | Rd ^ Rs → Rd. Encoding cross-checked against `XOR A0,A0 = 0x5600` in SPVU004 listing. |
| CLR Rd | Same word as `XOR Rd,Rd` | 1988 User's Guide page 12-51 | verified alias | tb_logical_flags | **Z=1 only; N/C/V Unaffected** | none | TBD | Exact `XOR Rd,Rd` alternate mnemonic. The shared XOR datapath and Z-only mask clear Rd and preserve N/C/V. |
| CMP Rs,Rd  | `0100 100S SSSR DDDD` | SPVU001A A-14 | implemented | tb_cmp_rr | N, C, Z, V | none | TBD | Flags from (Rd - Rs); **Rd unchanged** (nondestructive). First instruction in the project with `wb_reg_en = 0`. |
| JRcc short | `1100 cccc dddd dddd` | SPVU001A A-14, Table 12-8 (A0023) | implemented (16 of 16 cc codes) | tb_jruc_short, tb_jrcc_short, tb_jrcc_unsigned, tb_jrcc_signed, tb_jrcc_arith | none | none | TBD | Conditional/unconditional 8-bit-signed-disp relative jump. Recognized cc values: 0000 UC; 0001 LO; 0010 LS; 0011 HI; 0100 LT; 0101 GE; 0110 LE; 0111 GT; 1000 C(B); 1001 HS(NC); 1010 EQ; 1011 NE; 1100 V; 1101 NV; 1110 N; 1111 NN (Task 0101 added C/V/NV/N/NN single-flag tests). Codes 0001/0101/1010/1011 are context-shared aliases (P, Z/NZ-to-zero per Table 12-8); the single-flag decode matches the compare-form semantics. Target = PC_post_fetch + sign_extend(disp) × 16. disp ∈ {0x00, 0x80} reserved (long-form / absolute-form markers). |
| JRcc long  | `1100 cccc 0000 0000` + 16-bit signed disp | SPVU001A page 12-96 (Table 12-8 cc subset; A0023) | implemented (same 16 cc codes as short form) | tb_jrcc_long | none | none | TBD | ±32K-word conditional relative jump. Opcode word's low byte = `0x00` unlocks the long form; next 16-bit word is the signed displacement. Target = PC_after_both_fetches + sign_extend(disp16) × 16 (matches the spec's PC' definition). The related low-byte `0x80` absolute form is implemented as JAcc. |
| JUMP Rs    | `0000 0001 011R DDDD` (= `0x0160 \| (R<<4) \| Rs`) | SPVU001A page 12-98 + summary table | implemented | tb_jump_rs | **none** ("Unaffected" on all four per page 12-98) | none | TBD | Register-indirect jump: Rs → PC, with bottom 4 bits forced to 0 (word alignment). Single-word instruction. No status update. |
| DSJ Rd, Address | `0000 1101 100R DDDD` (= `0x0D80 \| (R<<4) \| Rd`) + 16-bit signed offset | SPVU001A page 12-70 | implemented | tb_dsj | **none** ("Unaffected" per page 12-70) | none | TBD | Decrement Rd; if `Rd != 0` after decrement, jump (PC' + offset×16); else fall through. Two-word instruction. |
| DSJEQ Rd, Address | `0000 1101 101R DDDD` (= `0x0DA0 \| (R<<4) \| Rd`) + 16-bit signed offset | SPVU001A page 12-72 | implemented | tb_dsj | **none** | none | TBD | If `Z=1`: DSJ semantics. If `Z=0`: no decrement, no jump, fall through. |
| DSJNE Rd, Address | `0000 1101 110R DDDD` (= `0x0DC0 \| (R<<4) \| Rd`) + 16-bit signed offset | SPVU001A page 12-73 | implemented | tb_dsj | **none** | none | TBD | If `Z=0`: DSJ semantics. If `Z=1`: no decrement, no jump, fall through. The DSJS single-word short form is also implemented (Task 0035). |
| JAcc Address | `1100 cccc 1000 0000` + 32-bit absolute address (LO, HI) | SPVU001A page 12-91 + summary table | implemented (same 16 cc codes as JRcc) | tb_jacc, tb_jrcc_arith | **none** ("Unaffected" per page 12-91) | none | TBD | Absolute conditional jump. Opcode low byte = `0x80` unlocks the absolute form. PC ← Address with bottom 4 bits forced to 0 (word alignment). Three-word instruction. Reuses needs_imm32 fetch path. |
| DSJS Rd, Address | `0011 1Dxx xxxR DDDD` (= `0x3800 \| (D<<10) \| (off<<5) \| (R<<4) \| Rd`) | SPVU001A page 12-74 + summary table | implemented | tb_dsjs | **none** ("Unaffected" per page 12-74) | none | TBD | Decrement-and-skip-jump SHORT form: single-word, 5-bit unsigned offset, 1-bit direction (D=0 forward, D=1 backward). Target = PC' ± offset×16. Decrements Rd unconditionally; jumps iff post-decrement Rd != 0. |
| ADDK K,Rd | `0001 00KK KKKR DDDD` | 1988 User's Guide page 12-40 (A0018) | verified | tb_addk_subk | N, C, Z, V | none | TBD | K + Rd → Rd for K=1..32. Opcode K field zero represents 32; the assembler rejects literal zero. |
| SUBK K,Rd | `0001 01KK KKKR DDDD` | 1988 User's Guide page 12-251 (A0018) | verified | tb_addk_subk | N, C, Z, V | none | TBD | Rd - K → Rd for K=1..32. Opcode K field zero represents 32. C = borrow. |
| DEC Rd | Same word as `SUBK 1,Rd` | 1988 User's Guide page 12-61 | verified alias | tb_addk_subk | N, C, Z, V | none | TBD | Exact `SUBK 1,Rd` alternate mnemonic; the test asserts `DEC A0 = 0x1420` and executes the decrement datapath. |
| NEG Rd    | `0000 0011 101R DDDD` (bits[6:5]=01 in the unary family) | SPVU001A A-14 | implemented | tb_neg_not | N, C, Z, V | none | TBD | 0 - Rd → Rd. V=1 only when Rd was 0x8000_0000. |
| NOT Rd    | `0000 0011 111R DDDD` (bits[6:5]=11 in the unary family) | 1988 User's Guide page 12-171 | verified | tb_neg_not, tb_logical_flags | **Z only; N/C/V Unaffected** | none | TBD | ~Rd → Rd. |
| ADDI IW K,Rd | `0000 1011 000R DDDD` + 16-bit imm | SPVU001A A-14 | implemented | tb_immi_iw | N, C, Z, V | none | TBD | Rd + sign_extend(K16, 32) → Rd. Reuses MOVI IW IMM_LO fetch. |
| SUBI IW K,Rd | `0000 1011 111R DDDD` + 16-bit imm | SPVU001A A-14 | implemented | tb_immi_iw | N, C, Z, V | none | TBD | Rd - sign_extend(K16, 32) → Rd. C is borrow. |
| CMPI IW K,Rd | `0000 1011 010R DDDD` + 16-bit imm | SPVU001A A-14 | implemented | tb_immi_iw | N, C, Z, V | none | TBD | Flags from Rd - sign_extend(K16); Rd unchanged. `wb_reg_en = 0`. |
| SLA K,Rd | `0010 00KK KKKR DDDD` | 1988 User's Guide page 12-239 (A0019) | verified | tb_shift_k, tb_shift_flags | **N, C, Z, V** | none | TBD | Rd << K; K=0..31 stored directly. V=1 when the new sign or any shifted-out bit differs from the original sign. |
| SLL K,Rd | `0010 01KK KKKR DDDD` | 1988 User's Guide page 12-241 (A0019) | verified | tb_shift_k, tb_shift_flags | **C, Z; N/V Unaffected** | none | TBD | Logical Rd << K; K=0..31 stored directly. |
| SRA K,Rd | `0010 10kk kkkR DDDD`, `k=-K mod 32` | 1988 User's Guide page 12-243 (A0019) | verified | tb_shift_k, tb_shift_flags | **N, C, Z; V Unaffected** | none | TBD | Sign-extending Rd >>> K. Opcode bits 9:5 store the five-bit two's complement of K. |
| SRL K,Rd | `0010 11kk kkkR DDDD`, `k=-K mod 32` | 1988 User's Guide page 12-245 (A0019) | verified | tb_shift_k, tb_shift_flags | **C, Z; N/V Unaffected** | none | TBD | Logical Rd >> K. Opcode bits 9:5 store the five-bit two's complement of K. |
| RL  K,Rd | `0011 00KK KKKR DDDD` | 1988 User's Guide page 12-234 (A0019) | verified | tb_shift_k, tb_shift_flags | **C, Z; N/V Unaffected** | none | TBD | Rd ROL K; K=0..31 stored directly. |
| SLA Rs,Rd | `0110 000S SSSR DDDD` (= `0x6000 \| (Rs<<5) \| (R<<4) \| Rd`) | 1988 User's Guide page 12-240 (A0019) | verified | tb_shift_rr, tb_shift_flags | **N, C, Z, V** | none | TBD | Rd << Rs[4:0], with the same SLA overflow rule as the constant form. |
| SLL Rs,Rd | `0110 001S SSSR DDDD` | 1988 User's Guide page 12-242 (A0019) | verified | tb_shift_rr, tb_shift_flags | **C, Z; N/V Unaffected** | none | TBD | Logical Rd << Rs[4:0]. |
| SRA Rs,Rd | `0110 010S SSSR DDDD` | 1988 User's Guide page 12-244 (A0019) | verified | tb_shift_rr, tb_shift_flags | **N, C, Z; V Unaffected** | none | TBD | Sign-extending right shift by the five-bit two's complement of Rs[4:0]. |
| SRL Rs,Rd | `0110 011S SSSR DDDD` | 1988 User's Guide page 12-246 (A0019) | verified | tb_shift_rr, tb_shift_flags | **C, Z; N/V Unaffected** | none | TBD | Logical right shift by the five-bit two's complement of Rs[4:0]. |
| RL  Rs,Rd | `0110 100S SSSR DDDD` | 1988 User's Guide page 12-235 (A0019) | verified | tb_shift_rr, tb_shift_flags | **C, Z; N/V Unaffected** | none | TBD | Rd ROL Rs[4:0]. |
| ADDI IL K,Rd | `0000 1011 001R DDDD` + 32-bit imm | SPVU001A A-14 | implemented | tb_immi_il | N, C, Z, V | none | TBD | Rd + K32 → Rd. |
| SUBI IL K,Rd | `0000 1101 000R DDDD` + 32-bit imm | SPVU001A A-14 | implemented | tb_immi_il | N, C, Z, V | none | TBD | Rd - K32 → Rd. **Different base prefix from the rest of the IL family.** |
| CMPI IL K,Rd | `0000 1011 011R DDDD` + 32-bit imm | SPVU001A A-14 | implemented | tb_immi_il | N, C, Z, V | none | TBD | Flags from Rd - K32; Rd unchanged. |
| ANDI IL K,Rd | `0000 1011 100R DDDD` + complemented 32-bit extension | 1988 User's Guide page 12-43 | verified | tb_immi_il, tb_logical_flags | **Z only; N/C/V Unaffected** | none | TBD | ANDI and ANDNI share one hardware opcode. For ANDI, the assembler stores `~IL` in the extension words and hardware computes `Rd & ~extension`, yielding `Rd & IL`. |
| ANDNI IL K,Rd | Same opcode as ANDI + uncomplemented 32-bit extension | 1988 User's Guide page 12-45 | verified | tb_logical_flags | **Z only; N/C/V Unaffected** | none | TBD | Source-level ANDNI supplies IL directly; the shared hardware operation computes `Rd & ~IL`. |
| ORI  IL K,Rd | `0000 1011 101R DDDD` + 32-bit imm | 1988 User's Guide page 12-173 | verified | tb_immi_il, tb_logical_flags | **Z only; N/C/V Unaffected** | none | TBD | Rd \| K32 → Rd. |
| XORI IL K,Rd | `0000 1011 110R DDDD` + 32-bit imm | 1988 User's Guide page 12-256 | verified | tb_immi_il, tb_logical_flags | **Z only; N/C/V Unaffected** | none | TBD | Rd ^ K32 → Rd. |
| MOVE Rs, Rd  | `0100 11MS SSSR DDDD` (base 0x4C00) | SPVU001A page 12-126 (A0020) | implemented (incl. cross-file) | tb_move_rr | N = data[31], Z = (data==0), V = 0, C Unaffected | none | TBD | **Opcode corrected in Task 0058** — previously decoded at 0x9000, which is actually MOVE Rs,\*Rd+ (a store). Full 32-bit copy (not a field move; no F bit). M=bit[9] selects same-file (0) vs cross-file (1); R=bit[4] is the file for both (M=0) or the source file (M=1). Only MOVE that crosses register files; `rs_file` carries the source file. Tasks 0077–0079 completed arbitrary field sizes, FE extension, unaligned/straddling fields, and FS-aware stepping for the memory MOVE forms. |
| MOVX Rs, Rd | `1110 110S SSSR DDDD` (base 0xEC00) | SPVU001A page 12-162 | implemented | tb_movx_movy | **none** (all Unaffected) | none | TBD | `Rd.X (low 16) <- Rs.X`; Rd's Y half (high 16) unchanged. Pure register op; result composed in the rf_wr_data mux as `{old_Rd[31:16], Rs[15:0]}`. Same-file (Rs=instr[8:5], Rd=instr[3:0]). Used for packed 16-bit / XY-address handling. |
| MOVY Rs, Rd | `1110 111S SSSR DDDD` (base 0xEE00) | SPVU001A page 12-163 | implemented | tb_movx_movy | **none** (all Unaffected) | none | TBD | `Rd.Y (high 16) <- Rs.Y`; Rd's X half (low 16) unchanged. Result = `{Rs[31:16], old_Rd[15:0]}`. |
| ADDXY Rs, Rd | `1110 000S SSSR DDDD` (base 0xE000) | SPVU001A page 12-41 | implemented | tb_addxy_subxy | **N=(Xres==0), V=Xres[15], Z=(Yres==0), C=Yres[15]** | none | TBD | Dual 16-bit add: `Rd.X += Rs.X, Rd.Y += Rs.Y`, NO carry between halves (X=low16, Y=high16). XY datapath in core (separate 16-bit adders); flags encode graphics-clipping info. Used for XY-address / incremental figure drawing. |
| SUBXY Rs, Rd | `1110 001S SSSR DDDD` (base 0xE200) | 1988 User's Guide §4.3 page 4-11 and page 12-252 | verified (Task 0134) | tb_addxy_subxy | **N=(RsX==RdX), V=(signed RsX>signed RdX), Z=(RsY==RdY), C=(signed RsY>signed RdY)** | none | TBD | Dual 16-bit subtract: `Rd.X -= Rs.X, Rd.Y -= Rs.Y`, no borrow between halves. C/V compare signed 16-bit XY coordinates; the focused negative/positive vector explicitly distinguishes this from unsigned borrow. |
| CMPXY Rs, Rd | `1110 010S SSSR DDDD` (base 0xE400) | SPVU001A page 12-55 | implemented | tb_cmpxy | **N=(Xres==0), V=Xres[15], Z=(Yres==0), C=Yres[15]** (Xres=RdX-RsX, Yres=RdY-RsY) | none | TBD | **Nondestructive** XY compare — Rd is NOT written; only flags are set. Its C/V are the per-half result sign bits, distinct from SUBXY's signed source-greater-than comparisons. Reuses the XY subtract datapath and is verified against TI's examples. |
| MPYS Rs, Rd | `0101 110S SSSR DDDD` (base 0x5C00) | 1988 User's Guide pages 12-164/165 | verified (variable FS1; Task 0135 status audit) | tb_mpy, tb_mpy_fs1, tb_mpy_flags | **N/Z from the stored result**; C, V Unaffected | none | TBD | Signed multiply. Even Rd stores all 64 bits in {Rd,Rd+1}, so N/Z use the full product; odd Rd stores only the low 32 bits, so N/Z use that truncated word. `tb_mpy_flags` distinguishes the widths with positive-full/negative-low and nonzero-full/zero-low products. The Rs multiplier is an FS1-bit field, sign-extended; FS1=0 means 32. |
| MPYU Rs, Rd | `0101 111S SSSR DDDD` (base 0x5E00) | 1988 User's Guide pages 12-166/167 | verified (variable FS1; Task 0135 status audit) | tb_mpy, tb_mpy_fs1, tb_mpy_flags | **Z from the stored result**; N, C, V Unaffected | none | TBD | Unsigned multiply with the same even/odd stored-result rule as MPYS. Even Rd tests the 64-bit pair; odd Rd tests the retained low word. The Rs field is zero-extended according to FS1. |
| MODU Rs, Rd | `0110 111S SSSR DDDD` (base 0x6E00) | SPVU001A page 12-113 | implemented | tb_modu | **Z = (remainder==0) [Unaffected if Rs=0], V = (Rs=0)**; N, C Unaffected | none | TBD | Unsigned 32-bit modulo: `Rd mod Rs → Rd` (the divider's remainder; dividend = {0, Rd}). On Rs=0: V=1, Rd unchanged, Z left Unaffected via a runtime `effective_flag_mask` (the static decode mask can't express "Z unaffected only when Rs=0"). Single writeback. Reuses the divider (`CORE_DIVIDE`). |
| DIVS Rs, Rd | `0101 100S SSSR DDDD` (base 0x5800) | 1988 User's Guide pages 12-63/64 | verified (Task 0135 status audit) | tb_divs_mods, tb_div_flags | **N per quotient/0x80000000 rules, Z=(quotient==0), V=overflow**; C Unaffected | none | TBD | Signed divide (even Rd = 64-bit signed dividend). The unsigned divider receives operand magnitudes and the core sign-conditions outputs. DIVS preserves its destination on overflow; N remains defined for a negative quotient or the documented `0x80000000` result unless the divider took its divisor-zero/high-half early-overflow path. `tb_div_flags` covers signed-range overflow separately from raw divider overflow. |
| MODS Rs, Rd | `0110 110S SSSR DDDD` (base `0x6C00`) | 1988 User's Guide pages 12-112/113 | verified (Task 0135 status audit) | tb_divs_mods, tb_div_flags | **Z=(remainder==0), except Unaffected if Rs=0; V=quotient overflow; N/C Unaffected** | none | TBD | Signed 32-bit modulo; remainder takes the dividend's sign. A nonzero-divisor signed quotient overflow still produces and writes the valid remainder (for example, `INT_MIN % -1 = 0`), updates Z, and sets V. Divisor zero preserves Z and the destination value while setting V. |
| DIVU Rs, Rd | `0101 101S SSSR DDDD` (base 0x5A00) | SPVU001A page 12-69 | implemented | tb_divu | **Z = (quotient==0), V = overflow** (Rs=0 or quotient>32b); N, C Unaffected | none | TBD | Unsigned divide. Even Rd: 64-bit dividend {Rd, Rd+1} ÷ Rs → quotient in Rd, remainder in Rd+1; odd Rd: Rd ÷ Rs → quotient in Rd. On overflow the result registers are unchanged. Multi-cycle: a new `tms34010_divider` module (restoring, ~32+ cycles) runs in the new `CORE_DIVIDE` state; the even-Rd quotient/remainder pair-writeback reuses MPY's `pair_wb_step`. Read port 3 carries Rd+1 (the dividend low half). |
| CPW Rs, Rd | `1110 011S SSSR DDDD` (base 0xE600) | SPVU001A page 12-57 | implemented | tb_cpw | **V = 1 iff point outside window**; N, C, Z Unaffected | none | TBD | Compare Point to Window: compares the signed XY point in Rs against window corners WSTART=B5 / WEND=B6 (implied B-file operands), loading a 4-bit out-of-window code into `Rd[8:5]` = {Rs.Y>WEND.Y, WSTART.Y>Rs.Y, Rs.X>WEND.X, WSTART.X>Rs.X} (all else 0). Used for line/point trivial-reject clipping. **Needed a 3rd regfile read port** (point + WSTART + WEND simultaneously) — added to `tms34010_regfile.sv` (cheap on the flop-based file). Comparisons are signed (A0027 does NOT apply; spec states signed). Verified vs TI's example table. |
| MOVE Rs, *Rd | `1000 00FS SSSR DDDD` (base 0x8000) | SPVU001A page 12-127 (A0020) | implemented (FS 1..32 + FE) | tb_move_indirect, tb_move_field | **none** (all Unaffected) | **write** (32-bit) | TBD | Store: `mem[*Rd] <- Rs`. Rd is a bit-address pointer (read via rf_rs2, NOT written back); Rs is the data (rf_rs1). Single field write in CORE_MEMORY. **Task 0077 makes this field-size aware**: `mem_size = mv_fs` (the F-selected FS0/FS1; FS=0 means 32), low FS bits of Rs written; the memory model does the partial-word read-modify-write, so unaligned/straddling fields work. |
| MOVE *Rs, Rd | `1000 01FS SSSR DDDD` (base 0x8400) | SPVU001A page 12-135 (A0020) | implemented (FS 1..32 + FE) | tb_move_indirect, tb_move_field | N = data[31], Z = (data==0), V = 0, C Unaffected | **read** (32-bit) | TBD | Load: `Rd <- mem[*Rs]`. Rs is a bit-address pointer (rf_rs1); Rd receives the data. Single field read; `mem_rdata` is field-extended (`mv_load_data`) and flows to the regfile write port and the `flag_input` mux at WRITEBACK (implicit compare-to-0). **Task 0077 makes this field-size aware**: reads `mem_size = mv_fs` bits, then sign-extends (FE=1) or zero-extends (FE=0) to 32 bits; N/Z reflect the extended value. Unaligned/straddling fields handled by the memory model. |
| MOVE Rs, *Rd+ | `1001 00FS SSSR DDDD` (base 0x9000) | SPVU001A page 12-129 (A0020) | implemented (FS 1..32 + FE) | tb_move_indirect_incdec, tb_move_field | **none** (all Unaffected) | **write** (32-bit) | TBD | Postincrement store: `mem[Rd] <- Rs; Rd += FS`. The pointer (Rd) is written back at WRITEBACK (`rf_wr_data = mv_ptr_new`). Shared `move_mode` field + `mv_*` address helpers in the core. Task 0060; field-size aware as of Task 0077 (pointer steps by ±FS). |
| MOVE Rs, -*Rd | `1010 00FS SSSR DDDD` (base 0xA000) | SPVU001A page 12-130 (A0020) | implemented (FS 1..32 + FE) | tb_move_indirect_incdec, tb_move_field | **none** (all Unaffected) | **write** (32-bit) | TBD | Predecrement store: `Rd -= FS; mem[Rd] <- Rs`. `mem_addr = Rd-FS`; pointer Rd-FS written back. Task 0060; field-size aware as of Task 0077. |
| MOVE *Rs+, Rd | `1001 01FS SSSR DDDD` (base 0x9400) | SPVU001A page 12-139 (A0020) | implemented (FS 1..32 + FE) | tb_move_indirect_incdec, tb_move_field | N = data[31], Z = (data==0), V = 0, C Unaffected | **read** (32-bit) | TBD | Postincrement load: `Rd <- mem[Rs]; Rs += FS`. Pointer Rs+FS written via a CORE_MEMORY-time regfile write (`mv_load_ptr_wr`); field-extended data to Rd at WRITEBACK. If Rs==Rd, data wins (WRITEBACK after CORE_MEMORY). Task 0060; field-size aware as of Task 0077 (FE extension + ±FS step). |
| MOVE -*Rs, Rd | `1010 01FS SSSR DDDD` (base 0xA400) | SPVU001A page 12-143 (A0020) | implemented (FS 1..32 + FE) | tb_move_indirect_incdec, tb_move_field | N = data[31], Z = (data==0), V = 0, C Unaffected | **read** (32-bit) | TBD | Predecrement load: `Rs -= FS; Rd <- mem[Rs]`. `mem_addr = Rs-FS`; pointer Rs-FS written during CORE_MEMORY, field-extended data to Rd at WRITEBACK. **Rs==Rd: data wins** (spec 12-143). Task 0060; field-size aware as of Task 0077 (FE extension + ±FS step). |
| MOVE *Rs, *Rd | `1000 10FS SSSR DDDD` (base 0x8800) | SPVU001A page 12-137 (A0020) | implemented (FS 1..32) | tb_move_m2m, tb_move_m2m_field | **none** (all Unaffected) | **read + write** (FS-bit ×2) | TBD | Indirect-to-indirect: `mem[*Rd] <- mem[*Rs]`. Two-step CORE_MEMORY sequence via `mem_op_step` — step 0 reads the FS-bit field at mem[Rs] into `move_data_q`, step 1 writes its low FS bits to mem[Rd]. Both pointers unchanged (plain form). Field-size aware (Task 0079): both transactions use `mem_size = mv_fs`; no FE extension (mem→mem). Task 0061. |
| MOVE *Rs+, *Rd+ | `1001 10FS SSSR DDDD` (base 0x9800) | SPVU001A page 12-138 (A0020) | implemented (FS 1..32) | tb_move_m2m_incdec, tb_move_m2m_field | **none** (all Unaffected) | **read + write** (FS-bit ×2) | TBD | Postincrement indirect-to-indirect: `mem[Rd] <- mem[Rs]; Rs += FS; Rd += FS`. Source pointer Rs updated at the step-0 read ack (`m2m_src_wr`); dest pointer Rd at WRITEBACK. **Rs==Rd**: per spec 12-138 the data is written to the incremented location and the register steps once — the step-1 write address reads the already-updated Rs, and the WRITEBACK Rd write is suppressed when Rs==Rd. Field-size aware (Task 0079): `mem_size = mv_fs`, pointers step ±FS. Task 0062. |
| MOVE -*Rs, -*Rd | `1010 10FS SSSR DDDD` (base 0xA800) | SPVU001A page 12-138 (A0020) | implemented (FS 1..32) | tb_move_m2m_incdec, tb_move_m2m_field | **none** (all Unaffected) | **read + write** (FS-bit ×2) | TBD | Predecrement indirect-to-indirect: `Rs -= FS; Rd -= FS; mem[Rd] <- mem[Rs]`. `m2m_src_addr/m2m_dst_addr = pointer-FS`. Pointer updates as above. The Rs==Rd predecrement corner is not defined by the spec; the implementation single-steps the register (write address is the doubly-decremented value) — documented deviation (A0020). Field-size aware (Task 0079): `mem_size = mv_fs`, pointers step ±FS. Task 0062. |
| MOVE Rs, @DAddr | `0000 01F1 100R SSSS` + 32-bit addr (base 0x0580) | SPVU001A page 12-134 (A0020) | implemented (FS 1..32 + FE) | tb_move_abs, tb_move_offabs_field | **none** (all Unaffected) | **write** (FS-bit) | TBD | Store to absolute address: `mem[DAddr] <- low FS bits of Rs`. Same `0000 01F1` field-op family as SETF/SEXT/ZEXT (sub-op `instr[7:5]=100`); register Rs at `instr[3:0]`. 32-bit absolute bit-address = the two words after the opcode (LSBs first), via the imm32 fetch path; `mem_addr = imm32`. Field-size aware (Task 0078): `mem_size = mv_fs` (F = instr bit 9 selects FS0/FS1). Task 0063. |
| MOVE @SAddr, Rd | `0000 01F1 101R DDDD` + 32-bit addr (base 0x05A0) | SPVU001A page 12-153 (A0020) | implemented (FS 1..32 + FE) | tb_move_abs, tb_move_offabs_field | N = result[31], Z = (result==0), V = 0, C Unaffected | **read** (FS-bit) | TBD | Load from absolute address: `Rd <- mem[SAddr]`. Sub-op `instr[7:5]=101`; Rd at `instr[3:0]`. `mem_addr = imm32`; the field-extended value (`mv_load_data`) flows to the regfile write + flag_input at WRITEBACK (implicit compare-to-0). Field-size aware (Task 0078): reads `mem_size = mv_fs` bits, then FE sign/zero-extends. Task 0063. |
| MOVE Rs, *Rd(off) | `1011 00FS SSSR DDDD` + off16 (base 0xB000) | SPVU001A page 12-132 (A0020) | implemented (FS 1..32 + FE) | tb_move_offset, tb_move_offabs_field | **none** (all Unaffected) | **write** (FS-bit) | TBD | Store, register-indirect with signed offset: `mem[Rd + sext(off16)] <- low FS bits of Rs`. Rd pointer unchanged. off16 is the 2nd word (imm16, sign-extended); `mem_addr = rf_rs2_data + imm32`. Rs=instr[8:5], Rd=instr[3:0]. Field-size aware (Task 0078): `mem_size = mv_fs`. Task 0064. |
| MOVE *Rs(off), Rd | `1011 01FS SSSR DDDD` + off16 (base 0xB400) | SPVU001A page 12-147 (A0020) | implemented (FS 1..32 + FE) | tb_move_offset, tb_move_offabs_field | N = result[31], Z = (result==0), V = 0, C Unaffected | **read** (FS-bit) | TBD | Load, register-indirect with signed offset: `Rd <- mem[Rs + sext(off16)]`. Rs pointer unchanged. `mem_addr = rf_rs1_data + imm32`; the field-extended value (`mv_load_data`) flows to regfile write + flag_input at WRITEBACK. Rs=instr[8:5], Rd=instr[3:0]. Field-size aware (Task 0078): reads `mem_size = mv_fs` bits, then FE sign/zero-extends. Task 0064. |
| MOVE *Rs(off), *Rd+ | `1101 00FS SSSR DDDD` + off16 (base 0xD000) | 1988 User's Guide page 12-149 | verified (FS 1..32) | tb_move_off_m2m_postinc | **none** (all Unaffected) | **read + write** (FS-bit ×2) | TBD | Two-word memory-to-memory form: read at `Rs + sext(off16)`, write through the original Rd, leave Rs unchanged, then postincrement Rd by FS. Reuses the two-step M2M field path with a dedicated class so only the destination pointer updates. Verified for F0/F1, positive/negative offsets, FS=32/8/12, unaligned and straddling fields, and status preservation. |
| MOVE @SAddr, *Rd+ | `1101 01F0 000R DDDD` + 32-bit addr | 1988 User's Guide page 12-155 | verified (FS 1..32) | tb_move_abs_m2m_postinc | **none** (all Unaffected) | **read + write** (FS-bit ×2) | TBD | Three-word memory-to-memory form: fetch the absolute source bit address low word first, read an FS-bit field there, write through the original Rd, then postincrement Rd by FS. Verified for F0/F1, A/B destinations, FS=32/8/12, nonzero address high word, unaligned and straddling fields, and status preservation. |
| MOVB Rs, *Rd | `1000 110S SSSR DDDD` (base 0x8C00) | SPVU001A page 12-118 | implemented | tb_movb | **none** (all Unaffected) | **write** (8-bit) | TBD | Byte store: `mem[*Rd] <- low 8 bits of Rs`. A special MOVE form with FS forced to 8 (`decoded.force_byte`); no FS field in the encoding, no auto inc/dec. Reuses the INSTR_MOVE_FIELD_STORE datapath. Rs=instr[8:5], Rd=instr[3:0]. Task 0080. |
| MOVB *Rs, Rd | `1000 111S SSSR DDDD` (base 0x8E00) | SPVU001A page 12-120 | implemented | tb_movb | N = result[31], Z = (result==0), V = 0, C Unaffected | **read** (8-bit) | TBD | Byte load: `Rd <- sign_extend(mem[*Rs])`. MOVB loads are ALWAYS right-justified and sign-extended to 32 bits (force_byte ⇒ mv_fe=1), with implicit compare-to-0. Rs=instr[8:5] (pointer), Rd=instr[3:0]. Task 0080. |
| MOVB *Rs, *Rd | `1001 110S SSSR DDDD` (base 0x9C00) | SPVU001A page 12-137 | implemented | tb_movb | **none** (all Unaffected) | **read + write** (8-bit ×2) | TBD | Byte indirect-to-indirect: `mem[*Rd] <- mem[*Rs]` (8 bits). Reuses INSTR_MOVE_FIELD_M2M with FS=8. Task 0080. |
| MOVB Rs, *Rd(off) | `1010 110S SSSR DDDD` + off16 (base 0xAC00) | SPVU001A page 12-119 | implemented | tb_movb | **none** (all Unaffected) | **write** (8-bit) | TBD | Byte store with signed offset: `mem[Rd + sext(off16)] <- low 8 bits of Rs`. Reuses INSTR_MOVE_OFF_STORE with FS=8. Task 0080. |
| MOVB *Rs(off), Rd | `1010 111S SSSR DDDD` + off16 (base 0xAE00) | SPVU001A page 12-121 | implemented | tb_movb | N = result[31], Z = (result==0), V = 0, C Unaffected | **read** (8-bit) | TBD | Byte load with signed offset: `Rd <- sign_extend(mem[Rs + sext(off16)])`. Reuses INSTR_MOVE_OFF_LOAD with FS=8 (sign-extended). Task 0080. |
| MOVB Rs, @DAddr | `0000 0101 111R SSSS` + 32-bit addr (base 0x05E0) | SPVU001A page 12-122 | implemented | tb_movb | **none** (all Unaffected) | **write** (8-bit) | TBD | Byte store to absolute address: `mem[DAddr] <- low 8 bits of Rs`. In the `0000 01..` field-op family, sub-op `instr[7:5]=111`, store = bit9 0. Reuses INSTR_MOVE_ABS_STORE with FS=8. Task 0080. |
| MOVB @SAddr, Rd | `0000 0111 111R DDDD` + 32-bit addr (base 0x07E0) | SPVU001A page 12-123 | implemented | tb_movb | N = result[31], Z = (result==0), V = 0, C Unaffected | **read** (8-bit) | TBD | Byte load from absolute address: `Rd <- sign_extend(mem[SAddr])`. Sub-op `instr[7:5]=111`, load = bit9 1. Reuses INSTR_MOVE_ABS_LOAD with FS=8 (sign-extended). Task 0080. |
| MOVB *Rs(SOff), *Rd(DOff) | `1011 110S SSSR DDDD` + source off16 + destination off16 (base 0xBC00) | 1988 User's Guide pages 12-120/12-121 | implemented | tb_movb_multiword | **none** (all Unaffected) | **read + write** (8-bit ×2) | TBD | Three-word byte offset-to-offset copy. Source and destination offsets are independently sign-extended and added to their unchanged base registers. Reuses the two-step M2M path with FS forced to 8. Task 0120. |
| MOVB @SAddr, @DAddr | `0000 0011 0100 0000` + source addr32 + destination addr32 (base 0x0340) | 1988 User's Guide pages 12-123/12-124 | implemented | tb_movb_multiword | **none** (all Unaffected) | **read + write** (8-bit ×2) | TBD | Five-word byte absolute-to-absolute copy. Each address is fetched low word then high word; the source pair precedes the destination pair. Reuses the two-step M2M path with FS forced to 8. Task 0120. |
| PIXBLT L,L | `0000 1111 0000 0000` (= `0x0F00`) | 1988 User's Guide pages 12-186 through 12-191 | verified direction/context (Tasks 0162/0163) | tb_pixblt_ll, tb_graphics_array_edges, tb_pixblt_direction | **N/C/Z/V Undefined; deterministically preserved** | **read×2 + write** (PSIZE-bit × DX·DY) | TBD | Transfer a DY×DX source array with PPOP, plane mask, and transparency. Zero dimensions are quiescent. PBH/PBV select all four traversal orders, but software must supply each selected corner; reverse-X starts one pixel beyond the right edge. Separate result pointers return exact next-row SADDR/DADDR. |
| PIXBLT L,XY / XY,L / XY,XY | `0x0F20 / 0x0F40 / 0x0F60` | 1988 User's Guide pages 12-192 through 12-209 | verified status/window/context/direction (Tasks 0135/0162–0164) | tb_pixblt_xy, tb_pixblt_window, tb_pixblt_w1, tb_pixblt_w2, tb_graphics_array_edges, tb_pixblt_direction, tb_window_common_rect | XY destination: **V=window result**; N/C/Z Undefined for L,XY but Unaffected for XY,XY. XY,L: all Undefined. | **read×2 + write** (PSIZE-bit × DX·DY) | TBD | XY source/destination pointers convert through CONVSP/CONVDP plus OFFSET. The presence of either XY operand enables automatic PBH/PBV adjustment of both arrays from supplied top-left/default addresses; traversal then predecrements in X and/or subtracts pitch in Y. Completion remains the linear geometric next-row context. W=1 issues no pixel traffic and returns the exact intersection dimensions plus the PBH/PBV-selected common corner for XY destinations. |
| DRAV Rs,Rd | `1111 011S SSSR DDDD` (= `0xF600`) | SPVU001A page 12-67 | **implemented (all W modes)** | tb_drav, tb_drav_win | **V** (window modes; W=0 leaves V unaffected) | **read + write** (PSIZE-bit RMW) | TBD | Draw and Advance: writes COLOR1 (B9) to the pixel at Rd's XY (converted via CONVDP+OFFSET(B4)+PSIZE at EXECUTE → CORE_DRAV 2-step RMW with PPOP/T/PMASK), then Rd ← Rd+Rs as an XY add (X+X, Y+Y, no carry; reuses the ADDXY datapath) at CORE_WRITEBACK. Rs/Rd same file. **Per-pixel window CONTROL.W=1/2/3 implemented** (Task 0112): WSTART/WEND read at CORE_DRAV_SETUP_WIN, Rd's pixel tested. W=1 never draws (V=!inside, WVP if inside); W=2 draws iff inside (else V=1+WVP); W=3 draws iff inside (else V=1). The advance always happens; V/WVP via the shared fill_win_flag_wb/wvp_set path at CORE_WRITEBACK. Tasks 0111/0112. |
| LINE Z | `1101 1111 Z001 1010` (= `0xDF1A` / `0xDF9A`) | 1988 User's Guide pages 12-99 through 12-104 | implemented (all W modes) | tb_line, tb_line_win, tb_line_abort | **N/C/Z Undefined and preserved; V set by window operation** | **read + write** per pixel (PSIZE-bit RMW × COUNT) | TBD | Bresenham inner loop with implied B operands and d/DADDR/COUNT writeback. W=3 clips; W=2 and W=1 abort on their miss/hit conditions. V follows the last evaluated window result; undefined N/C/Z are deterministically preserved. |
| PIXBLT B,L / B,XY | `0x0F80 / 0x0FA0` | 1988 User's Guide pages 12-176 through 12-185 | verified status/window/context/direction isolation (Tasks 0135/0162–0164) | tb_pixblt_b, tb_pixblt_window, tb_pixblt_w1, tb_pixblt_w2, tb_graphics_array_edges, tb_pixblt_direction, tb_window_common_rect | B,L: **all Undefined and preserved**. B,XY: **N/C/Z Undefined; V=window result (Undefined at W=0)** | **read×2 + write** (1-bit src; PSIZE-bit dst) | TBD | Binary source color expansion selects COLOR0/COLOR1 before processing. Both instruction pages explicitly ignore PBH/PBV, so source bits and destination pixels always advance in increasing-address order. Zero dimensions are traffic-free and completion returns next-row context. B,XY W=1 returns the common rectangle's lowest-address corner and dimensions without source or destination traffic. |
| FILL L | `0000 1111 1100 0000` (= `0x0FC0`) | SPVU001A page 12-80 | verified empty/context (Task 0162) | tb_fill_l, tb_fill_ppop, tb_graphics_array_edges | **none** (all Unaffected) | **read+write** (PSIZE-bit × DX·DY) | TBD | Fill a DY×DX pixel array with COLOR1 (B9), starting at DADDR (B2), rows DPTCH (B3) bits apart. Zero DX or DY performs no traffic or DADDR writeback. The nonempty loop applies `merged = PPOP(COLOR1, dest)` with plane mask/transparency; final DADDR is the next X pixel on the final row, including signed/non-unit pitches. |
| FILL XY | `0000 1111 1110 0000` (= `0x0FE0`) | 1988 User's Guide pages 12-84 through 12-86 | verified (Tasks 0133/0135/0162/0164) | tb_fill_xy, tb_fill_window, tb_fill_w1, tb_fill_w2, tb_graphics_array_edges, tb_window_common_rect | **N/C/Z Unaffected; V=window result, Unaffected at W=0** | **write** (PSIZE-bit × DX·DY) | TBD | Zero DX or DY performs no conversion writeback, traffic, or status change. Otherwise initial XY DADDR converts to a linear start and final DADDR is the linear next-X address following the last final-row pixel. W=1 issues no pixel traffic and returns the exact common rectangle's lowest-address DADDR plus DYDX; W=2 requires containment, and W=3 clips per pixel. Task 0135 added V=1 when W=3 required any preclipping (V=0 when fully inside) while preserving N/C/Z. |
| CVXYL Rs, Rd | `1110 100S SSSR DDDD` (base 0xE800) | SPVU001A page 12-59 | implemented | tb_cvxyl | **none** (all Unaffected) | none | TBD | Convert XY address to linear: `Rd = [(Y << (31-CONVDP[4:0])) \| (X << log2(PSIZE))] + OFFSET`. X=Rs[15:0], Y=Rs[31:16] (signed). OFFSET = B-file B4 (read on regfile port 3); CONVDP/PSIZE from the I/O registers (taps). Shifts (screen pitch and pixel size are powers of two). Validated against TI's example table. Rs/Rd same file. Task 0084. |
| PIXT Rs, *Rd | `1111 100S SSSR DDDD` (base 0xF800) | SPVU001A §"PIXT" (Pixel Transfer - Reg to Indirect) | implemented (linear, replace mode) | tb_pixt | **none** (all Unaffected) | **write** (PSIZE-bit) | TBD | Pixel store: `mem[*Rd] <- low PSIZE bits of Rs`. Rd = linear (pixel-aligned) bit address. First graphics op — a field store with `force_pixel` ⇒ FS = PSIZE I/O register (1/2/4/8/16). A 2-step read-modify-write pixel engine: step 0 reads the dest pixel, step 1 writes `merged = (processed & ~PMASK) \| (dest & PMASK)` where `processed = PPOP(src, dest)`. **PPOP (Tasks 0091/0092)**: all 22 CONTROL.PPOP ops — 16 Boolean + 6 arithmetic (ADD/ADDS-sat/SUB/SUBS-sat/MAX/MIN on the unsigned PSIZE-bit pixel). **Transparency (Task 0089→0091)**: T=1 + processed pixel 0 ⇒ dest left unchanged. **Plane mask (Task 0090)**: per-plane protect. At reset (PPOP=0, T=0, PMASK=0) this is plain replace. Rs=instr[8:5], Rd=instr[3:0]. Task 0083. |
| PIXT *Rs, Rd | `1111 101S SSSR DDDD` (base 0xFA00) | SPVU001A §"PIXT" (Pixel Transfer - Indirect to Reg) | implemented (linear) | tb_pixt | **V = (pixel != 0)**; N, C, Z Undefined (masked off) | **read** (PSIZE-bit) | TBD | Pixel load: `Rd <- zero_extend(mem[*Rs])` to 32 bits (PSIZE-bit field; zero-extend, unlike MOVB's sign-extend). Rs = linear pointer. `force_pixel` ⇒ FS = PSIZE, FE = zero. Only V is defined (pixel nonzero); the decode masks N/C/Z off. Task 0083. |
| PIXT *Rs, *Rd | `1111 110S SSSR DDDD` (base 0xFC00) | SPVU001A §"PIXT" (Pixel Transfer - Indirect to Indirect) | implemented (linear, replace mode) | tb_pixt | **none** (all Unaffected) | **read + write** (PSIZE-bit ×2) | TBD | Pixel copy: `mem[*Rd] <- mem[*Rs]` (PSIZE bits). Reuses INSTR_MOVE_FIELD_M2M with `force_pixel`. Replace mode only. Task 0083. |
| PIXT Rs, *Rd.XY | `1111 000S SSSR DDDD` (base 0xF000) | SPVU001A §"PIXT" | implemented (replace mode; all W modes) | tb_pixt_xy, tb_pixt_win | **V** (window modes; W=0 Unaffected) | **write** (PSIZE-bit) | TBD | XY-addressed pixel store: Rd holds an XY value, converted to a linear address via CONVDP + OFFSET(B4) + PSIZE (the `xy_addr` path, same shift form as CVXYL), then the pixel from Rs is stored. force_pixel ⇒ FS=PSIZE. Full pixel engine: PPOP boolean ops + transparency + plane mask (2-step RMW, Tasks 0089-0091). **Per-pixel window checking** (Task 0117): CORE_PIXT_SETUP_WIN reads WSTART/WEND, the pointer's XY is tested; W=2/3 draw inside, W=1 never; out-of-window inhibited; V=NOT-inside, WVP on W=1 hit/W=2 miss (gated by pixt_xy_win). Task 0085. |
| PIXT *Rs.XY, Rd | `1111 001S SSSR DDDD` (base 0xF200) | SPVU001A §"PIXT" | implemented | tb_pixt_xy | **V = (pixel != 0)**; N, C, Z Undefined (masked off) | **read** (PSIZE-bit) | TBD | XY-addressed pixel load: Rs holds an XY value, converted via CONVSP + OFFSET(B4) + PSIZE (source pitch), then the pixel is loaded into Rd, zero-extended. V = pixel nonzero. Task 0085. |
| PIXT *Rs.XY, *Rd.XY | `1111 010S SSSR DDDD` (base `0xF400`) | 1988 User's Guide pages 12-223 through 12-225 | verified window/status (Task 0135) | tb_pixt_xy, tb_pixt_win | **N/C/Z Unaffected; V=outside for W=1/2/3, Unaffected at W=0** | **read + write** (PSIZE-bit ×2) | TBD | Both pointers convert through CONVSP/CONVDP plus OFFSET. Task 0135 extended the destination-window path to this form: W=1 suppresses every write, W=2/W=3 write only when inside, V reports outside, and W=1 hit/W=2 miss request WV. |
| NOP          | `0000 0011 0000 0000` (= `0x0300`) | SPVU001A §"NOP" page 12-170 (A0021) | implemented | tb_nop | **none** ("processor status is otherwise unaffected") | none | TBD | No operation — PC advances to the next instruction, ST and registers untouched. Distinct from the unary family at `0000 0011 1xxx xxxx`; ABS A0 = `0x0380`, not `0x0300`. |
| ABS Rd    | `0000 0011 100R DDDD` (= `0x0380 \| (R<<4) \| Rd`) | SPVU001A page 12-34 | implemented (spec-correct after A0024 RESOLVED in Task 0037) | tb_abs_negb, tb_btst | N, Z, V (C truly **Unaffected** via wb_flag_mask) | none | TBD | `Rd ← \|Rd\|` via ALU_OP_ABS (conditional select between `a` and `0-a` based on the sign of `0-a`). V=1 when Rd was 0x80000000 (MIN_INT — `\|Rd\|` can't be represented; spec returns Rd unchanged). N reflects the sign of `0-Rd`, NOT the sign of the result. |
| NEGB Rd   | `0000 0011 110R DDDD` (= `0x03C0 \| (R<<4) \| Rd`) | SPVU001A page 12-168 | implemented | tb_abs_negb | N, C, Z, V | none | TBD | `Rd ← -Rd - C` via ALU_OP_SUBB with alu_a=0, alu_b=Rd. Used in sequence with NEG/SUB/SUBB/SUBI for multi-register negation. Spec page 12-168 provides 12 worked example vectors; tb_abs_negb uses four of them. |
| BTST K, Rd  | `0001 11KK KKKR DDDD` (= `0x1C00 \| (K<<5) \| (R<<4) \| Rd`) | SPVU001A page 12-46 + summary table | implemented | tb_btst | **Z only** (N/C/V truly Unaffected via wb_flag_mask) | none | TBD | Test bit K of Rd (K in 0..31). Z = 1 if bit K of Rd is 0, else Z = 0. Rd not written. Uses ALU_OP_AND with alu_b = `32'd1 << K`. |
| BTST Rs, Rd | `0100 101S SSSR DDDD` (= `0x4A00 \| (Rs<<5) \| (R<<4) \| Rd`) | SPVU001A page 12-47 + summary table | implemented | tb_btst | **Z only** (N/C/V Unaffected) | none | TBD | Same as BTST K but bit index comes from Rs[4:0]. Uses ALU_OP_AND with alu_b = `32'd1 << rf_rs1_data[4:0]`. |
| CLRC | `0x0320` (single fixed encoding) | SPVU001A summary table page A-14 | implemented | tb_st_ops | **C only** (cleared; N, Z, V Unaffected via wb_flag_mask) | none | TBD | Clear ST.C to 0. Uses constant-flags mux + wb_flag_mask = c-only. |
| SETC | `0x0DE0` (single fixed encoding) | SPVU001A summary table | implemented | tb_st_ops | **C only** (set; N, Z, V Unaffected) | none | TBD | Set ST.C to 1. Symmetric to CLRC. |
| GETST Rd | `0000 0001 100R DDDD` (= `0x0180 \| (R<<4) \| Rd`) | SPVU001A summary table | implemented | tb_st_ops | none (status unaffected) | none | TBD | Rd ← ST (32-bit copy of the status register). regfile-write-data mux routes `st_value` for this iclass. |
| PUTST Rs | `0000 0001 101R DDDD` (= `0x01A0 \| (R<<4) \| Rs`) | SPVU001A summary table | implemented | tb_st_ops | N, C, Z, V (whatever the source register contains at those bit positions) | none | TBD | ST ← Rs (full 32-bit write). Uses the existing `st_write_en` + `st_write_data` path on the status register; the in-Rs N/C/Z/V bits become the new ST flags. |
| GETPC Rd | `0000 0001 010R DDDD` (= `0x0140 \| (R<<4) \| Rd`) | SPVU001A summary table | implemented | tb_pc_ops | none | none | TBD | Rd ← current PC (bit-addressed, captured at CORE_WRITEBACK). |
| EXGPC Rd | `0000 0001 001R DDDD` (= `0x0120 \| (R<<4) \| Rd`) | 1988 User's Guide page 12-79 | verified (Task 0132) | tb_pc_ops | none (N/C/Z/V Unaffected, full-ST checked) | none | TBD | Atomic swap with the next PC: PC ← `(old Rd & ~0xF)` and Rd ← next PC. The focused test supplies unaligned `0x0000064F` and proves execution at `0x00000640`; the async read port supplies old Rd during same-cycle writeback. |
| REV Rd   | `0000 0000 001R DDDD` (= `0x0020 \| (R<<4) \| Rd`) | 1988 User's Guide page 12-233 | verified (Task 0132) | tb_pc_ops | none (N/C/Z/V Unaffected, full-ST checked) | none | TBD | Rd ← `0x0000_0008`, the exact TMS34010 revision value in the guide's worked example. |
| LMO Rs, Rd | `0110 101S SSSR DDDD` (= `0x6A00 \| (Rs<<5) \| (R<<4) \| Rd`) | SPVU001A page 12-108 + summary table | implemented | tb_lmo | **Z only** (N/C/V Unaffected via wb_flag_mask) | none | TBD | Rd ← 31 - bit_pos(leftmost-1 in Rs) in bottom 5 bits (upper 27 bits = 0). If Rs = 0: Rd = 0 and Z = 1. Otherwise Z = 0. Useful for normalization/priority encoding. The 5 spec-table worked examples are the test vectors in tb_lmo. |
| SETF FS,FE,F | `0000 01F1 01FE FFFFF` | SPVU001A page 12-237 + summary table | implemented | tb_setf | **none** (Unaffected) | none | TBD | Set the F-selected field-size pair: F=0 updates FS0/FE0; F=1 updates FS1/FE1. FS is 5-bit (00000 encodes field-size 32). FE is 1-bit (0=zero-ext, 1=sign-ext). The core constructs the new ST value by splicing the F-selected slot with the literal bit-fields from the instruction word and uses the existing `st_write_en` path. Other ST bits (flags, IE, PBX, the other field slot) are preserved. |
| SEXT Rd, F | `0000 01F1 000R DDDD` (= `0x0500 \| (F<<9) \| (R<<4) \| Rd`) | SPVU001A page 12-238 + summary table | implemented | tb_sext_zext | N, Z (C, V Unaffected via wb_flag_mask) | none | TBD | Sign-extend the low `FS<F>` bits of Rd to 32 bits. F bit at instr[9] selects FS0 (F=0) or FS1 (F=1) from ST. FS = 5'b00000 encodes field-size 32 (identity). |
| ZEXT Rd, F | `0000 01F1 001R DDDD` (= `0x0520 \| (F<<9) \| (R<<4) \| Rd`) | SPVU001A page 12-256 + summary table | implemented | tb_sext_zext | **Z only** (N, C, V Unaffected) | none | TBD | Zero-extend the low `FS<F>` bits of Rd to 32 bits. Same F-bit + FS source as SEXT. |
| EXGF Rd, F | `1101 01F1 000R DDDD` (= `0xD500 \| (F<<9) \| (R<<4) \| Rd`) | SPVU001A page 12-77 + summary table | implemented | tb_exgf | **none** (Unaffected) | none | TBD | Atomic swap: Rd[5:0] ↔ {FE<F>, FS<F>} in ST. Rd's upper 26 bits cleared after the swap. Core uses async-read rf_rs2_data to read the OLD Rd value while writing the new (FE:FS) value in the same WRITEBACK cycle. |
| DINT | `0x0360` (single fixed encoding) | SPVU001A summary table | implemented | tb_dint_eint | **none** (Unaffected) | none | TBD | Clear ST.IE (bit 21) — disable maskable interrupts. Implemented via the full-ST-write path with `st_value & ~(1<<21)`. |
| EINT | `0x0D60` (single fixed encoding) | SPVU001A summary table | implemented | tb_dint_eint | **none** (Unaffected) | none | TBD | Set ST.IE (bit 21) — enable maskable interrupts. Symmetric to DINT. |
| EMU | `0x0100` (single fixed encoding) | 1988 User's Guide pages 12-77 and 2-10 | verified through physical pin (Task 0152) | tb_emu, tb_emu_bridge, tb_pin_system | indeterminate in the original device; deterministically preserved here (A0032) | none | one exact Q1/Q2 pulse in RUN; repeated Q1/Q2 indication while halted; Q3/Q4 reserved for HLDA | The abstract core retains Task 0128's active-low execute-cycle/halt handshake. Task 0152 synchronizes physical RUN/EMU, transfers each execute event through a held CDC request, phase-latches halt, and combines Q1/Q2 EMUA with Q3/Q4 HLDA on the original shared output. RUN retires as a NOP; EMU remains memory-quiescent with PC at the following instruction and resumes when RUN returns high. |
| PUSHST | `0x01E0` (single fixed encoding) | SPVU001A summary table page A-16 | implemented | tb_pushst, tb_popst | **none** (Unaffected) | **write** (32-bit) | TBD | `SP <- SP - 32; mem[SP] <- ST`. First user of the memory-write infrastructure (Task 0047): introduces `decoded.needs_memory_op` + the active CORE_MEMORY FSM state + 32-bit writes through the sim memory model. ALU computes the new SP via SUB with alu_b = 32. Single-cycle memory write thanks to the model's word-pair atomic ack. |
| POPST  | `0x01C0` (single fixed encoding) | SPVU001A summary table page A-16 | implemented | tb_popst | **N, C, Z, V from popped value** | **read** (32-bit) | TBD | `ST <- mem[SP]; SP <- SP + 32`. Inverse of PUSHST. Reads 32-bit ST from `mem[SP]` (the OLD SP value, NOT the post-increment); ALU computes `SP + 32` for the regfile writeback; the popped value flows directly into `st_write_data` (via the existing st_write path extended for POPST). All four flags are set from the popped value's bits 28-31. |
| CALL Rs | `0000 1001 001R DDDD` (= `0x0920 \| (R<<4) \| Rs`) | SPVU001A page 12-47 + summary table | implemented | tb_call_rs, tb_rets | **none** (Unaffected) | **write** (32-bit; pushes PC') | TBD | `SP -= 32; mem[SP] <- PC'; PC <- Rs` (with bottom 4 bits of PC forced to 0). PC' = address of the instruction following CALL. First subroutine-call instruction. Reuses the memory-write infrastructure from PUSHST plus the PC-load mux pattern from JUMP Rs. |
| RETS [N] | `0000 1001 011N NNNN` (= `0x0960 \| N`) | SPVU001A page 12-231 + summary table | implemented | tb_rets, tb_calla_callr | **none** (Unaffected) | **read** (32-bit; pops PC) | TBD | `PC <- mem[SP]; SP <- SP + 32 + 16*N`. N at instr[4:0] (0..31) — optional argument-pop count after return. The popped PC value flows directly into the PC-load mux (no bottom-nibble mask, since pushed PCs are already word-aligned). |
| CALLA Address | `0x0D5F` (single fixed) + 32-bit absolute target | SPVU001A page 12-48 + summary table | implemented | tb_calla_callr | **none** (Unaffected) | **write** (32-bit; pushes PC') | TBD | 3-word instruction. `SP -= 32; mem[SP] <- PC'; PC <- address` (with bottom 4 bits cleared). Target reuses `branch_target_jacc` from JAcc. PC' = bit-address of the instruction following the 3-word CALLA. |
| CALLR Address | `0x0D3F` (single fixed) + 16-bit signed disp | SPVU001A page 12-49 + summary table | implemented | tb_calla_callr | **none** (Unaffected) | **write** (32-bit; pushes PC') | TBD | 2-word instruction. `SP -= 32; mem[SP] <- PC'; PC <- PC' + sign_ext(disp16)*16`. Target reuses `branch_target_long` from JRcc long form. PC' = bit-address of the instruction following the 2-word CALLR. |
| RETI | `0x0940` (single fixed encoding) | SPVU001A page 12-230 + summary table | implemented | tb_reti | **N, C, Z, V from popped ST** (full ST restored) | **read** ×2 (32-bit; pops ST then PC) | TBD | `ST <- mem[SP]; SP += 32; PC <- mem[SP]; SP += 32`. First multi-transaction memory instruction. CORE_MEMORY adds a `mem_op_step` counter that sequences two 32-bit reads (step 0 at `SP`, step 1 at `SP+32`) and latches the values into `popped_st_q` / `popped_pc_q`. The FSM transition to CORE_WRITEBACK gates on `mem_op_step == 1`. ALU computes `SP + 64` for the regfile writeback. The popped ST flows through the existing `st_write_data` mux (so the full 32-bit ST including all flag bits is restored atomically). |
| TRAP N | `0000 1001 000N NNNN` (= `0x0900 \| N`) | SPVU001A page 12-252 + summary table | implemented (all N, 0..31) | tb_trap, tb_trap0 | **N=0, C=0, Z=0, V=0** (ST forced to `0x00000010`) | N>0: **write** ×2 + **read** ×1; N=0: **read** ×1 | TBD | Software-interrupt sequence: `SP -= 32; mem[SP] <- PC'; SP -= 32; mem[SP] <- ST; ST <- 0x00000010; PC <- mem[0xFFFFFFE0 - N*32]`. First three-transaction instruction: `mem_op_step` cycles 0→1→2 with per-step direction (write/write/read) and per-step address (SP-32 / SP-64 / vector). The bounded sim memory aliases N>0 vector addresses through its low index bits; its reset/TRAP 0 word has dedicated `level0_vector` storage as of Task 0121. ALU computes `SP - 64`. Post-TRAP ST is the spec-fixed `0x10` (IE=0, FS0=16, FS1=0, flags=0). **TRAP 0** (`trap_skip_push`) is special per SPVU001A page 12-253 note 1: it does NOT push PC'/ST and does NOT decrement SP — only fetches the vector at `0xFFFFFFE0` and sets ST. Implemented by collapsing the 3-step sequence to a single vector-fetch step when `decoded.k5 == 0`, and gating `alu_b` to 0 so SP is unchanged. |
| Illegal opcode | All reserved ranges in Table 8-6, including `0x0200`–`0x02FF` | 1988 User's Guide §8.7, page 8-9, Table 8-6 | implemented | tb_illegal_opcode | live ST forced to `0x00000010`; old ST stacked | **write** ×2 + **read** ×1 | TBD | Unmaskable TRAP-30-equivalent entry: architecturally reserved words bypass execute, push post-opcode PC and old ST, decrement SP by 64 bits, and fetch the handler PC from `0xFFFF_FC20`. `illegal_trap` distinguishes these ranges from valid-but-unimplemented words; the broader sticky `illegal_opcode_o` remains available for decoder diagnostics. |
| MMTM Rp, list | `0000 1001 100R DDDD` + 16-bit mask | SPVU001A page 12-111 + summary table | implemented | tb_mmtm, tb_mmtm_nflag | **N = sign of (0 - original Rp) = ~Rp[31]** (Task 0057); C, Z, V Unaffected (`wb_flag_mask` N-only) | up to 16 32-bit writes | TBD | First data-dependent transaction count: 1..16 32-bit writes driven by `popcount(mask)`. Iterates set bits of mask LSB → MSB; for each, `Rp -= 32` then `mem[Rp] <- R(bit_idx)`. Final Rp = address of the lowest write. Shared iterator state (Task 0056 renamed `mmtm_*` → `mm_*`): `mm_rp_q` (working Rp), `mm_mask_q` (residual mask, bits cleared as processed), `mm_iter_idx` (priority-encoded set bit — lowest for MMTM). `rf_rs1_idx` is multiplexed during CORE_MEMORY to point at the current iter register so the regfile's async read serves as the per-cycle data source. **N flag (Task 0057):** spec defines N = sign of (0 - Rp) with exceptions Rp=0→1 and Rp=0x80000000→0; the closed form is N = ~Rp[31], computed in the `flag_input` mux from `rf_rs2_data` (which still reads the original Rp during WRITEBACK because the regfile read returns the pre-write value). Bit-to-register mapping per assumption A0026 (bit N = R(N)); the spec's chart is a graphical figure that didn't survive pdftotext extraction. |
| MMFM Rp, list | `0000 1001 101R DDDD` + 16-bit mask | SPVU001A page 12-109 + summary table | implemented | tb_mmfm | **none** (N, C, Z, V all Unaffected per spec) | up to 16 32-bit reads | TBD | Pop counterpart of MMTM; reverses its action. Iterates set bits of mask MSB → LSB (highest-order register restored first); for each, `R(bit_idx) <- mem[Rp]` then `Rp += 32` — the post-increment happens after **every** read including the last, so final Rp = initial Rp + 32·count (points one word past the restored block). Shares the `mm_rp_q` / `mm_mask_q` / `mm_iter_idx` iterator with MMTM; `mm_iter_idx` selects the highest set bit for MMFM. Each restored register is written through the regfile write port during CORE_MEMORY (a second user of that port besides the WRITEBACK final-Rp write) via the `mmfm_pop_wr` path, with `rf_wr_idx = mm_iter_idx` and `rf_wr_data = mem_rdata`; Rp is never in the list (spec: "unpredictable results"), so the two write users never collide. Validated absolutely against TI's worked example on page 12-110 (B-file) and for MMTM/MMFM mask-mapping consistency via an A-file round-trip. |

## Coverage categories

The table currently covers the implemented move/load/store, arithmetic,
logical, shift/rotate, field, branch/call/return, stack/trap/interrupt, and
graphics operations. Continue to add one row with a specification citation
and named test for every newly recognized instruction.

- Move / load / store (MOVE, MOVB, MOVI, MOVK, etc.)
- Arithmetic (ADD, ADDC, ADDI, ADDK, SUB, SUBI, SUBK, NEG, ABS, CMP, ...)
- Logical (AND, ANDI, OR, ORI, XOR, XORI, NOT, ...)
- Shift / rotate (SLA, SLL, SRA, SRL, RL, ...)
- Field operations (MOVE field, EXGF, ...)
- Branches and jumps (JR, JRcc, JUMP, CALL, RETS, ...)
- Stack (PUSH, PUSHST, POP, POPST, ...)
- Graphics (PIXT, PIXBLT, FILL, LINE, DRAV, ...)
- Control (NOP, EMU, EINT, DINT, RETI, TRAP, ...)

No explicit decode gap remains in the MOVB family. The complete official
summary-table reconciliation and the remaining system exit gates are in
`completion_audit.md`. This table is an implementation ledger, not proof that
the complete TMS34010 ISA or all original-silicon timing behavior is covered.
The complete individual-page status review is in `status_audit.md`;
`tb_status_decode` exhaustively checks its static mask policy across all
65,536 opcode words.
